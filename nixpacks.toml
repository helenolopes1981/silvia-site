# nixpacks.toml
[phases.setup]
nixPkgs = ["nodejs-20_x", "nodePackages.pnpm"] # ou npm

[phases.install]
cmd = "pnpm install" # ou npm install

[phases.build]
cmd = "pnpm run build" # ou npm run build

[start]
cmd = "pnpm run preview -- --host 0.0.0.0 --port $PORT"
